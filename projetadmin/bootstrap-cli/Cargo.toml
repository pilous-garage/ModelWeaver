[package]
name = "modelweaver-bootstrap-cli"
version = "0.6.0"
description = "ModelWeaver Bootstrap CLI (headless installer)"
authors = ["ModelWeaver Team"]
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
