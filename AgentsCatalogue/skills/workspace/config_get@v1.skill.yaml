name: workspace/config_get@v1
category: workspace
description: Lit une ou toutes les configs d'un workspace.
inputs:
  workspace_id:
    type: string
    required: true
  key:
    type: string
    required: false
    description: 'Clef spécifique (ex: provider, model). Vide = toutes.'
outputs:
  ok:
    type: boolean
  config:
    type: object
implementation:
  type: python
  function: workspacedb.project.config_get
