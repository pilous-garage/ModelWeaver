name: util/get_env@v1
category: util
description: Lit une variable d'environnement depuis une allowlist sécurisée.
inputs:
  name:
    type: string
    required: true
    description: Nom de variable (PATH, HOME, USER, LANG, MODELWEAVER_HOME, ...)
outputs:
  value:
    type: string
  allowed:
    type: boolean
implementation:
  type: python
  function: system.env.get_env
