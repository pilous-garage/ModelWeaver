name: system/get_env@v1
category: system
description: "Lit une variable d'environnement depuis une allowlist sécurisée."
inputs:
  name:
    type: string
    required: true
    description: "Nom de variable (PATH, HOME, USER, LANG, MODELWEAVER_HOME, ...)"
outputs:
  value:
    type: string
  allowed:
    type: boolean
implementation:
  type: python
  function: _exec_get_env
