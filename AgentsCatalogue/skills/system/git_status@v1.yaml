name: system/git_status@v1
category: system
description: "Affiche le statut git (--porcelain) du projet."
inputs:
  project_id:
    type: string
    required: true
    description: "Identifiant du projet"
outputs:
  stdout:
    type: string
  clean:
    type: boolean
  exit_code:
    type: integer
implementation:
  type: python
  function: _exec_git_status
