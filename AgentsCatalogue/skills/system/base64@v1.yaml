name: system/base64@v1
category: system
description: "Encode ou décode une chaîne en base64."
inputs:
  data:
    type: string
    required: true
    description: "Chaîne à traiter"
  mode:
    type: string
    required: false
    default: encode
    description: "encode | decode"
outputs:
  result:
    type: string
implementation:
  type: python
  function: system.text.base64