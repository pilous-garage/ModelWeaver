name: system/git_push@v1
category: system
description: "Publie les commits. En local (même dépôt) les commits sont déjà partagés ; push un remote si configuré."
inputs:
  project_id:
    type: string
    required: true
    description: "Identifiant du projet"
outputs:
  stdout:
    type: string
  stderr:
    type: string
  exit_code:
    type: integer
implementation:
  type: python
  function: _exec_git_push
