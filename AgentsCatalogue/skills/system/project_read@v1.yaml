name: system/project_read@v1
category: system
description: "Lit un fichier de l'espace projet partagé."
inputs:
  project_id:
    type: string
    required: true
    description: "Identifiant du projet"
  agent_id:
    type: string
    required: false
    description: "Identifiant de l'agent (injecté automatiquement par le FSM si absent)"
  path:
    type: string
    required: true
    description: "Chemin relatif dans le projet"
outputs:
  content:
    type: string
implementation:
  type: python
  function: system.project.project_read