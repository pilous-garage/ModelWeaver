name: file/write_file@v1
category: file
description: "Écrase un fichier dans le home de l'agent avec du nouveau contenu (boundé par safe_path — ne peut pas sortir du home). Préférez patch_file pour modifier une partie spécifique."
inputs:
  path:
    type: string
    required: true
    description: Chemin relatif au home de l'agent
  content:
    type: string
    required: true
    description: Contenu complet à écrire (écrase l'existant)
outputs:
  result:
    type: string
implementation:
  type: python
  function: file.ops.write_file
